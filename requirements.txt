# What the brain needs. Versions are the ones it has actually been run against.
#
# After installing these, one more step:  python -m playwright install chromium
#
# Then fetch the connectome (see README for the exact files, ~1.1 GB, CC-BY,
# no account needed) and run:  py build_graph.py

numpy==2.4.2
scipy==1.17.1
pandas==3.0.1
pyarrow==23.0.1          # pandas needs it to read the .feather annotations
requests==2.32.5
Pillow==12.2.0           # decodes the screenshots the retina samples

fastapi==0.128.0
uvicorn[standard]==0.40.0

playwright==1.62.0       # the browser the sim drives
